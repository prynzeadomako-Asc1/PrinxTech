# Optional: OpenAI API key (put in backend environment if you have one)
OPENAI_API_KEY=

# Frontend: API base url (adjust if backend runs on a different host/port)
VITE_API_BASE_URL=http://localhost:8000
